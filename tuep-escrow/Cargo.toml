[package]
name = "tuep-escrow"
version = "0.1.0"
edition = "2021"
description = "TUEP / BDPE escrow LAW — types, clocks, payout vectors, typed events, state machine"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
blake3 = "1.5"

[dev-dependencies]
