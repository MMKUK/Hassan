# ── Build artifacts ─────────────────────────────────────────────
/target/
/dist/

# ── Editor / tool backups ───────────────────────────────────────
**/*.rs.bk
**/*.bak
*.bak
*.tmp
*~
*.orig
*.swp
*.swo

# ── Runtime chain state (regenerated by the node) ───────────────
hassan-data/
*.bin
# macOS Finder duplicate copies ("file 2.bin")
* [0-9].bin
* [0-9].bak
chainstate*.bak*

# ── Keystores / secrets — NEVER commit ──────────────────────────
my-wallet.json
wallet.json
*.keystore
*.aek
*.key
*.pem
id_*
secrets*

# ── Logs ────────────────────────────────────────────────────────
*.log

# ── OS cruft ────────────────────────────────────────────────────
.DS_Store
.localized

# ── Editor ──────────────────────────────────────────────────────
.vscode/
*.code-workspace

# ── Superseded / duplicate versions & broken drafts ─────────────
src_v1.4/
Cargo_v1.4.toml
genesis_v1.4.toml
genesis.toml.broken

